/**
 * Local email/password sign-in (this app's Better Auth DB — not the broker).
 *
 * Enabled for PandaOS admin access. Sign-in forms use `authClient.signIn.email`
 * from `@/lib/auth/client`. Do NOT edit `server.ts` for this.
 */
export const emailAndPasswordEnabled = true;
